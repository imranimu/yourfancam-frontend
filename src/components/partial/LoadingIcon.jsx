import React, { Component } from 'react';

export default class LoadingIcon extends Component {
    render() {
        return (
        <img src="/images/loader.svg" alt="Form Submit load icon." />
        );
    }
}
